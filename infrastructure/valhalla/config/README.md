# Valhalla Config

Store Valhalla configuration templates here.

Future files may include:

- `valhalla.json`
- environment-specific notes
- tile build configuration

Do not commit secrets or local-only generated configuration. Files matching `*.local.*` in this directory are ignored.

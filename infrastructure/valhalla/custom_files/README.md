# Custom Files

This directory is reserved for future Valhalla support files such as boundaries, costing inputs, or campus-specific routing support data.

Local files in this directory are ignored by Git unless explicitly promoted into documented infrastructure.
